---
interface Props {
  size?: number
  alt?: string
  class?: string
}
const { size = 24, alt = "Qibla Compass", class: className = "" }: Props = Astro.props
---

<img src="../../icons/prayer/qibla-compass/fill.svg" {alt} width={size} height={size} class={className} />
